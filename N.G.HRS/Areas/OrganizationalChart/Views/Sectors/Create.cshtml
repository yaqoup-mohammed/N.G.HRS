@model N.G.HRS.Areas.OrganizationalChart.Models.Sectors
@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers

@{
    ViewData["Title"] = "Create";
    Layout = "_Layout";

}

<h1>Create</h1>

<h4>Sectors</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="alert-danger"></div>
            <div class="form-group">
                <label asp-for="SectorsName" class="control-label"></label>
                <input asp-for="SectorsName" class="form-control" />
                <span asp-validation-for="SectorsName" class="alert-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Notes" class="control-label"></label>
                <input asp-for="Notes" class="form-control" />
                <span asp-validation-for="Notes" class="alert-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="BranchesId" class="control-label"></label>
                <select asp-for="BranchesId" id="branches" class="js-example-basic-single form-select" asp-items="@(ViewData["branches"] as SelectList)">
                    <option value=""> </option>
                </select>
            </div>
            <div class="form-group">
                <input type="submit" id="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
    <script>
        document.getElementById("submit").addEventListener("click", function (event) {
            const branches = document.getElementById("branches");

            if (branches.value === "") {
                event.preventDefault(); // Prevent form submission
                Swal.fire({
                    icon: "error",
                    title: "خطئ....",
                    text: "رجاء أختر  الفرع",
                });
            }
        });
    </script>
}
