@model N.G.HRS.Areas.PayRoll.Models.EmployeeAdvances
@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers

@{
    ViewData["Title"] = "Create";
    Layout = "_Layout";

}

<h1>Create</h1>

<h4>EmployeeAdvances</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="EmployeeId"  class="control-label"></label>
                <select asp-for="EmployeeId" id="EmployeeId" class="js-example-basic-single form-select" asp-items="@(ViewData["EmployeeId"] as SelectList)">
                    <option value="">  </option>
                </select>
            </div>
            <div class="form-group">
                <label asp-for="DepartmentId"  class="control-label"></label>
                <select asp-for="DepartmentId" id="DepartmentId" class="js-example-basic-single form-select" asp-items="@(ViewData["DepartmentId"] as SelectList)">
                    <option value="">  </option>
                </select>
            </div>
            <div class="form-group">
                <label asp-for="SectionId"  class="control-label"></label>
                <select asp-for="SectionId" id="SectionId" class="js-example-basic-single form-select" asp-items="@(ViewData["Sections"] as SelectList)">
                    <option value="">  </option>
                </select>
                <span asp-validation-for="SectionId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="EmployeeAccountId"  class="control-label"></label>
                <select asp-for="EmployeeAccountId" id="EmployeeAccountId" class="js-example-basic-single form-select" asp-items="@(ViewData["EmployeeAccountId"] as SelectList)">
                    <option value="">  </option>

                </select>
            </div>
            <div class="form-group">
                <label asp-for="CurrencyId"  class="control-label"></label>
                <select asp-for="CurrencyId" class="js-example-basic-single form-select" asp-items="@(ViewData["CurrencyId"] as SelectList)">
                    <option value="">  </option>

                </select>
            </div>
            <div class="form-group">
                <label asp-for="Amount" class="control-label"></label>
                <input asp-for="Amount" class="form-control" />
                <span asp-validation-for="Amount" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Notes" class="control-label"></label>
                <input asp-for="Notes" class="form-control" />
                <span asp-validation-for="Notes" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" id="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}


@* =================================================================== *@
 <script>
     $(document).ready(function () {

         $('#EmployeeId').change(function () {
             var id = this.value;
             if (id != null) {
                 $.ajax({
                     url: '/PayRoll/EmployeeAdvances/LoadData?id=' + id,
                     type: 'GET',
                     success: function (data) {
                            $('#SectionId').val(data.sectionsId).change();
                         $('#DepartmentId').val(data.departmentsId).change();
                     }
                 });
                 // $.ajax({
                 //        url: '/PayRoll/EmployeeAdvances/LoadAccount?id=' + id,
                 //     type: 'GET',
                 //     success: function (data) {
                 //         $.each(data, function (i, item) {
                 //                $('#EmployeeAccountId').append('<option value="' + item.id + '">' + item.financeAccount+' < /option>')
                 //         });
                 //     }
                 // });

             }

         });

         @* =================================================================== *@
             document.getElementById("").addEventListener("click", function (event) {
                 const CurrencyId = document.getElementById("CurrencyId");
                 if (CurrencyId.value === "") {
                     event.preventDefault(); // Prevent form submission
                     Swal.fire({
                         icon: "error",
                         title: "خطئ....",
                         text: "رجاء أختر العملة  ",
                     });
                 }
             });

         @* ================================================ *@
             document.getElementById("").addEventListener("click", function (event) {
                 const DepartmentId = document.getElementById("DepartmentId");
                 if (DepartmentId.value === "") {
                     event.preventDefault(); // Prevent form submission
                     Swal.fire({
                         icon: "error",
                         title: "خطئ....",
                         text: "رجاء أختر الادارة  ",
                     });
                 }
             });

         @* ================================================ *@
             document.getElementById("").addEventListener("click", function (event) {
                 const EmployeeId = document.getElementById("EmployeeId");
                 if (EmployeeId.value === "") {
                     event.preventDefault(); // Prevent form submission
                     Swal.fire({
                         icon: "error",
                         title: "خطئ....",
                         text: "رجاء أختر الموظف  ",
                     });
                 }
             });
     });
</script>
}